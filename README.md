# Bot
a simple terminal chatbot written in python, just for fun. It  utilizes the openai api key for prompt responses

CREATE VIRTUAL ENVIRONMENT

python3 -m venv venv

INSTALL REQUIREMENTS

python install -r requirements.txt




